//create function to accept arr size and arr
class GameofLife {

  constructor(size, grid) {
    this.size = size;
    this.currGen = grid;
  }

  this.init = (size, grid) => {
    var deadCount = 0;

  }
}
